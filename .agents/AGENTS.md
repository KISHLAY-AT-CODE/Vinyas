# Vinyas Workspace Rules

## Release Checklist
- **CRITICAL**: Before any production release, deployment, or git push of the application, explicitly remind the user to verify if any testing localhost configurations or local endpoints have been left in active source code, and confirm if they should be replaced with production endpoints.
